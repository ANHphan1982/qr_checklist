# =============================================================================
# frontend/.env — Copy file này thành .env rồi điền giá trị thật
# KHÔNG commit file .env lên GitHub
# =============================================================================

# URL backend Render sau khi deploy
# Ví dụ: https://qr-checklist-api.onrender.com
# Để trống khi dev local (Vite proxy tự forward /api → localhost:5000)
VITE_API_URL=https://your-api.onrender.com
